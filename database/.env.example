DATABASE_URL=postgresql://coachx:coachx@localhost:5432/coachx_dev?schema=public

# Optional — used only by backend/tests/integration/database.integration.test.ts.
# Point this at a DEDICATED test database (never coachx_dev, never a
# production URL) to run the real-database integration suite locally
# or in CI. Left unset, those tests self-report as SKIPPED rather than
# silently no-oping — see docs/database/TESTING.md.
# TEST_DATABASE_URL=postgresql://coachx:coachx@localhost:5432/coachx_test?schema=public
