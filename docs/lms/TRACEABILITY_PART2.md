# Traceability — Phase 6 Part 2 (004 FR-017–060 and adjacent)

Status values used below (mandated enum): **IMPLEMENTED** / **PARTIALLY IMPLEMENTED** / **DEFERRED TO PART 3** / **OWNED BY ANOTHER FEATURE** / **BLOCKED BY FEATURE 003 FRONTEND** / **BLOCKED BY FEATURE 009 ENTITLEMENT** / **BLOCKED BY DECISION** / **NOT APPLICABLE TO PART 2**.

A requirement is marked IMPLEMENTED only when the runtime rule is enforced end-to-end (not just a field/type/route stub), server-verified, and covered by at least a unit test. See each row's note for exactly what is/isn't covered.

## Correction pass summary

A follow-up mandatory correction and verification pass re-examined this table against fresh spec.md `grep` evidence (not solely against the previous version of this document) and corrected six rows: FR-021 (unchanged, re-verified), FR-028 (capacity enforcement now implemented), FR-029 (re-confirmed deferred, with an honest reason — not a decision-gate substitute for mandatory work), FR-052 (multi-condition support implemented; `ALL_ACTIVITIES_VIEWED`'s MANUAL-alias defect corrected), FR-056 (idempotency infrastructure genuinely integrated). See `docs/lms/COMPLETION_ENGINE.md`, `ENROLLMENT_LIFECYCLE.md`, `PROGRESS_ENGINE.md`, and `DECISION_GATES.md` gates #37–41 for the full detail behind each correction.

| FR | Requirement (summary) | Status | Note |
|---|---|---|---|
| FR-017 | Lesson content types | PARTIALLY IMPLEMENTED | 7 of ~17 listed types modeled (VIDEO/AUDIO/ARTICLE/PDF/DOWNLOAD/EXTERNAL_LINK/EMBED). Quiz/Assessment/Assignment/Project/Survey/Reflection/Live-session DEFERRED TO PART 3 or owned by another feature; SCORM/Interactive-tool/AI-exercise explicitly out of scope per spec.md's own "future-ready" list. |
| FR-018 | Lesson field set | PARTIALLY IMPLEMENTED | Core fields present (title/slug/type-via-activity/description/module/order/duration/mandatory/preview/completion-rule/status/version). Transcript, captions, resources-list, discussion-enabled, notes-enabled NOT built. |
| FR-019 | Lesson player UI | BLOCKED BY FEATURE 003 FRONTEND | No authenticated member frontend exists to host a player — see "Frontend decision" below. Backend content API exists. |
| FR-020 | Course overview / Continue Learning CTA | PARTIALLY IMPLEMENTED | `getContinueLearning` backend algorithm IMPLEMENTED and tested; the overview screen's UI is BLOCKED BY FEATURE 003 FRONTEND. |
| FR-021 | Access grant sources | PARTIALLY IMPLEMENTED | Full 10-value taxonomy modeled; only FREE and ADMIN_GRANT actually resolve to ALLOWED. The other 8 are BLOCKED BY FEATURE 009 ENTITLEMENT (fail-closed, see `ENTITLEMENT_BOUNDARY.md`). |
| FR-022 | Server-side entitlement check on every content request | IMPLEMENTED | `evaluateLessonAccess` runs on every `/me/lessons/:id` GET, progress POST, and complete POST — never only at enrollment time. |
| FR-023 | Access states | PARTIALLY IMPLEMENTED | Enrolled/Active/Completed/Expired/Revoked/Suspended/Prerequisite-incomplete all representable. "Refunded" OWNED BY ANOTHER FEATURE (009); "Eligible-not-enrolled" is a computed `AccessDecision`, not a stored state. |
| FR-024 | Access expiry models | PARTIALLY IMPLEMENTED | Fixed-days/lifetime via `accessStartAt`/`accessEndAt` IMPLEMENTED. "Until membership active"/"until program end"/"until org license ends" BLOCKED BY FEATURE 009 ENTITLEMENT (no membership/program/org entity exists). |
| FR-025 | Enrollment flow order | PARTIALLY IMPLEMENTED | Entitlement + prerequisite(course-level: none) + duplicate-check + create + notify(none) implemented; capacity check and terms/declaration capture NOT built (no capacity fields exist on Course). |
| FR-026 | Enrollment denial reasons | PARTIALLY IMPLEMENTED | Membership/payment-required (via UNAVAILABLE), course-full N/A (no capacity), enrollment-closed (ENROLLMENT_PAUSED/ARCHIVED/RETIRED), account-restricted N/A. Region-restricted/org-license-unavailable BLOCKED BY FEATURE 009/003. |
| FR-027 | Wishlist | DEFERRED TO PART 3 | Not attempted — no User-facing catalog save/wishlist entity. |
| FR-028 | Capacity controls | PARTIALLY IMPLEMENTED (corrected this pass) | The specific "maximum enrollments" clause is now IMPLEMENTED — `Course.enrollmentLimit` is enforced at enrollment-creation time (`countSeatOccupyingEnrollments`, `COURSE_FULL` rejection), verified by an integration test. "Reserved seats," "organization allocation," and "manual override" sub-clauses remain DEFERRED TO PART 3 (no reservation/organization-allocation concept exists). |
| FR-029 | Waitlist | DEFERRED TO PART 3 | Re-confirmed during the correction pass: a genuinely separate entity (join date, priority, referral source, notification status, time-limited offer expiry, offer-passes-to-next-user background logic) — not reasonable to build inside a correction pass on top of an already-large phase. This is a scope/size judgment, not a claim that it is spec-optional (FR-029 says "MUST maintain a Waitlist") — it is honestly reported as DEFERRED, not silently reclassified as out-of-scope. |
| FR-030 | Refund access/certificate policy | OWNED BY ANOTHER FEATURE | Feature 009. |
| FR-031 | Course retirement effects | PARTIALLY IMPLEMENTED | RETIRED blocks new enrollment (course-lifecycle.policy.ts, Part 1). Access-deadline/replacement-suggestion/progress-export/archive-copy NOT built. |
| FR-032 | Bulk CSV enrollment | DEFERRED TO PART 3 | Not attempted. |
| FR-033 | Organization admin capabilities | OWNED BY ANOTHER FEATURE | No Organization/Team entity exists in this codebase (spec.md's own Assumption). |
| FR-034 | Drip release rules | PARTIALLY IMPLEMENTED (corrected this pass) | IMMEDIATE/DAYS_AFTER_ENROLLMENT/FIXED_DATE/AFTER_PREVIOUS_MODULE fully evaluated. INSTRUCTOR_RELEASE now FULLY IMPLEMENTED end-to-end (read-path evaluation PLUS the write endpoint that was previously missing — see `PREREQUISITES_AND_RELEASE.md` and `DECISION_GATES.md` gate #23). Cohort-schedule and assessment-pass release types excluded (no Cohort/Assessment entity). |
| FR-035 | Locked content display | PARTIALLY IMPLEMENTED | Backend `AccessDecision.detail` carries `unlockAt`/`prerequisiteModuleId` for a client to render; no frontend renders it (BLOCKED BY FEATURE 003 FRONTEND). |
| FR-036 | Prerequisite types | PARTIALLY IMPLEMENTED | Module-completion prerequisite IMPLEMENTED. Assessment-pass/assignment-approval/membership-level/mentor-approval/admin-approval DEFERRED TO PART 3 or OWNED BY ANOTHER FEATURE (007/009). |
| FR-037 | Circular prerequisite prevention | IMPLEMENTED | Reused unchanged from Part 1 (`module.service.ts`'s `assertValidPrerequisite`). |
| FR-038 | Sequencing modes | PARTIALLY IMPLEMENTED (corrected this pass) | Module-level Flexible and Instructor-Controlled are FULLY IMPLEMENTED — "instructor manually releases content" now has a real, ownership-checked, audited write endpoint (`POST /instructor/modules/:id/release`), not just a read-path evaluation. A strict per-lesson "Sequential" mode is still NOT built (see `ACCESS_DECISION_ENGINE.md`'s explicit scope boundary — Lesson has no releaseRuleType field) — this is a distinct, still-valid deferral, unrelated to the INSTRUCTOR_RELEASE fix. |
| FR-039–045 | Video lesson runtime features (playback controls, tracking, streaming, captions, signed URLs) | NOT APPLICABLE TO PART 2 | Part 2A brief: "LearningActivity is metadata only, no runtime engines." |
| FR-046 | Audio lesson runtime features | NOT APPLICABLE TO PART 2 | Same as above. |
| FR-047 | Text lesson rendering/TOC/reading-time | PARTIALLY IMPLEMENTED | `bodyText` stored and served; no TOC/reading-time computation, no translation variants. |
| FR-048 | PDF viewer | NOT APPLICABLE TO PART 2 | Metadata only (`mediaUrl`, `fileSizeBytes`). |
| FR-049 | Downloadable resources + view/download events | PARTIALLY IMPLEMENTED | `DOWNLOAD` activity type exists; no resource-viewed/download-started event tracking. |
| FR-050–051 | Live class + attendance | NOT APPLICABLE TO PART 2 | No provider integration; out of Part 2A/2B scope entirely. |
| FR-052 | Lesson completion rule types | PARTIALLY IMPLEMENTED (corrected this pass) | MANUAL, MINIMUM_WATCH_PERCENT, ALL_ACTIVITIES_VIEWED, INSTRUCTOR_APPROVAL all FULLY_SUPPORTED with real runtime evaluation — see `COMPLETION_ENGINE.md`'s rule support matrix (`ALL_ACTIVITIES_VIEWED`'s prior MANUAL-alias defect is corrected; a real `ActivityProgress` model now backs it). Multi-condition combination ("MUST allow a single lesson to combine multiple required conditions") is now FULLY_SUPPORTED via the new additive `Lesson.completionRuleTypes` array field, AND-evaluated. Quiz-pass/assignment-submit/assignment-approved/live-attendance BLOCKED_PENDING_PART3 (entities don't exist); external-activity-confirmation NOT_SUPPORTED (no clear owning mechanism); AI-exercise-completion OWNED_BY_ANOTHER_FEATURE (008). |
| FR-053 | Never complete solely from "video opened" | IMPLEMENTED | Verified by `completion.service.ts` design (no code path marks complete from a view/open event) and by unit tests on the entitlement/access layers; no dedicated "video-opened-does-not-complete" integration test was added (would require a real DB) but the logic path structurally cannot produce this outcome — `maybeAutoCompleteFromProgress` only fires on `watchedPercent` crossing a threshold, which requires an explicit progress POST with that value. |
| FR-054 | Course progress calculation model | PARTIALLY IMPLEMENTED | Mandatory-only weighting (the spec's own "recommended default") IMPLEMENTED. Equal-weight/duration-weighted/instructor-assigned-weight alternatives NOT offered as configurable options. |
| FR-055 | Progress at lesson/module/course/path/program levels | PARTIALLY IMPLEMENTED | Lesson/module/course IMPLEMENTED. Path/Program levels NOT APPLICABLE TO PART 2 (no Path/Program entity exists anywhere in this codebase). |
| FR-056 | Cross-device idempotent sync | PARTIALLY IMPLEMENTED (corrected this pass) | Idempotent server-authoritative upsert with anti-rollback IMPLEMENTED (unchanged). CORRECTED: the shared `IdempotencyKey` table is now genuinely integrated for enrollment creation, lesson completion, completion overrides, and progress resets (see `ENROLLMENT_LIFECYCLE.md`/`COMPLETION_ENGINE.md`); a high-frequency progress heartbeat deliberately does NOT force one `IdempotencyKey` row per call (documented two-tier strategy in `PROGRESS_ENGINE.md`). Offline queue, device-source tagging, and mobile app still do not exist — DEFERRED TO PART 3 / OWNED BY ANOTHER FEATURE (no mobile app in this codebase at all). |
| FR-057 | Learning streak | DEFERRED TO PART 3 | Not attempted (spec.md Assumptions: LMS-local streak tracking is a documented future item; broader gamification is 006's). |
| FR-058 | Learner notes | DEFERRED TO PART 3 | Not attempted. |
| FR-059 | Bookmarks | DEFERRED TO PART 3 | Not attempted. |
| FR-060 | Per-lesson discussion | OWNED BY ANOTHER FEATURE | 005 (Community). |
| FR-105–109 | Learning analytics / event taxonomy | DEFERRED TO PART 3 | Not attempted — `recordAuditEvent` calls exist for state-changing actions only (audit trail, not an analytics event stream); Part 2B's own "do not audit every playback heartbeat" instruction was followed. |
| FR-113 | Progress override (mark complete/reset, reason, audit, permission) | IMPLEMENTED | `overrideMarkComplete`/`overrideReset`, both admin and instructor-scoped (instructor restricted to LESSON/MODULE, never COURSE). |
| FR-122 | Security controls (entitlement, signed media, malware scan, rate limits, IDOR isolation) | PARTIALLY IMPLEMENTED | Server-side entitlement (IMPLEMENTED), instructor-scope IDOR isolation (IMPLEMENTED, tested), input sanitization via Zod (IMPLEMENTED). Signed media URLs and malware scanning are NOT APPLICABLE TO PART 2 (no file-hosting pipeline exists). |

## Member/admin frontend decision (re-verified per Part 2C)

Re-checked at implementation time: `frontend/` still has no authenticated member session/dashboard route (Feature 003's member-facing authenticated area was not present as of this phase, matching the audit's Phase 1–5 findings), and `admin/` still has no LMS operations UI beyond what Part 1 already declared out of scope. Per the brief's explicit instruction ("implement complete backend APIs; do not pretend authenticated learner pages are functional"), **no frontend UI was built in Part 2** — this is a re-confirmed, not newly-discovered, limitation. All Part 2 work is backend API + tests + documentation only, exactly as Part 1 was for its own admin-UI portion.

## Database verification pass (added, does not change any FR status above)

A dedicated pass attempted to execute every DB-gated behavior in this table against a real PostgreSQL database. PostgreSQL/Docker remain unavailable in this sandbox (verified fresh: `docker --version`, `docker compose version`, `pg_isready`, `psql --version`, PATH lookups, common Windows install-directory checks, and `prisma migrate status` itself failing with `P1001`). No row in this table changes from PARTIALLY IMPLEMENTED/IMPLEMENTED to a "database-verified" status as a result — every "IMPLEMENTED" marking in this document has always meant "the runtime rule is enforced end-to-end and covered by at least a unit test" (per this file's own header definition), never "verified against a live database," and that distinction is unchanged. One row DID change status this pass on its own merits: FR-034/FR-038 (INSTRUCTOR_RELEASE write endpoint, previously the one incomplete piece of an otherwise-implemented rule) — see the updated rows above and `DECISION_GATES.md` gate #23.
