---
description: "Task list for Feature 004 — Learning Management System: Courses, Assessments & Certification"
---

# Tasks: Learning Management System: Courses, Assessments & Certification

**Input**: Design documents from `/specs/004-learning-management-system/` (`spec.md`, `plan.md`)

**Prerequisites**: plan.md, spec.md, and **Features 001–003's Foundational phases complete** (User Account/RBAC/EntitlementGuard/Content Governance from 001, Consent from 002, Auth/Profile/Account-Status from 003).

**Tests**: Included throughout — this feature is the constitution's primary cited implementation of Article IX (Action Before Consumption) and directly implements Article I (server-authoritative completion/entitlement) and Article VIII (no vanity-metric completion); the no-vanity-completion and circular-prerequisite guarantees get dedicated contract tests in Foundational.

**Organization**: Tasks are grouped by user story (US1–US9 from spec.md), plus two supplementary cross-cutting phases (Discovery & Recommendations; Learning Analytics & At-Risk Detection) covering FR groups that don't map to a single story, following the same pattern established in 003's Profile Management phase.

## Format: `[ID] [P?] [Story] Description`

**Revision note (2026-07-28)**: First verification pass against real code. An extensive, pre-existing, separately-documented LMS implementation already lives in `backend/src/lms/` (built earlier, referred to in its own docs as "Phase 6 Part 1/2/2B/2C") — it does NOT use this file's `backend/src/modules/lms-*/` layout; it is a flat `backend/src/lms/` module, its own internally-consistent architecture. A dedicated audit (Explore agent, full file reads + grep, not guesswork) found: Foundational's content hierarchy/enrollment/drip/completion-engine are strongly built; US1 (Enrollment) and US2 (Lesson Consumption) are the closest to complete; **US3 (Quiz), US4 (Assignment), and US5 (Certificate) are entirely unbuilt — zero models, zero services** — despite spec.md marking all 5 as P1/MVP-critical. Given the user's explicit scope decision this session ("US1 completion gaps + US2 lesson-player UI only") this pass: (1) built FR-033 organization-admin course assignment (`backend/src/lms/org-assignment.service.ts` — now buildable since `001` added `Organization`, which didn't exist when Part 2's docs recorded this as "owned by another feature"); (2) built the real lesson-player frontend (`frontend/src/pages/LessonPlayerPage.tsx` + a new `backend/src/lms/curriculum.service.ts` sidebar endpoint) wired to the pre-existing, genuinely solid completion engine; (3) wired the enroll CTA into `CourseDetailPage.tsx`, absent since Part 1 predated Part 2's enrollment work. US3/US4/US5 remain **entirely deferred** — each is its own future specs-scale effort, not attempted here. Every task below is checked only where re-verified this pass or confidently evidenced by the audit; everything else is left unchecked with a reason (pre-existing gap / explicitly deferred this session / overlaps a future feature).

---

## Phase 1: Setup

- [x] T001 [P] Verified this pass — 001/002/003's Foundational work is deployed and actively consumed (e.g. `org-assignment.service.ts` reuses `001`'s `Organization`; the frontend's `RequireAuth`/`auth.context.tsx` from `003` gate the new `/learn/*` routes).
- [ ] T002 Not resolved — no video/CDN provider, live-class provider, or similarity-detection vendor is selected or needed for this pass's scope (no video/audio infrastructure or live-class integration was in scope). FR-041's watch-percentage default of 80% and FR-054's mandatory-only weighting are already implemented as defaults in the pre-existing `completion.service.ts`/`progress.service.ts` — the remaining open items (FR-081 certificate threshold, FR-099 version policy, FR-092 offline-submission approval) all belong to the still-unbuilt Certificate/Course-Versioning/Offline features.
- [~] T003 [P] No literal `backend/src/modules/{...}` path — this repo's real, established architecture is flat `backend/src/lms/` (same documented mismatch pattern as specs 001-003's own module-path notes). `curriculum.service.ts` and `org-assignment.{service,validation,controller}.ts` were added there this pass, following the existing convention, not the task-file's aspirational one.

---

## Phase 2: Foundational (Blocking Prerequisites)

**⚠️ CRITICAL**: No user story work can begin until this phase is complete.

- [~] T004 `Course`/`CourseModule`/`Lesson`/`LearningActivity`/`CourseInstructor`/`CourseCategory`/`CourseVersion` all exist (`database/prisma/schema.prisma`). **`LearningPath`/`Program`/`Cohort`/`LearningPathCourse` do NOT exist** — confirmed via full-schema grep; `docs/lms/DECISION_GATES.md` gate #27 documents this as out of scope for all Feature 004 phases so far. Not addressed this pass (large, separate entities beyond "US1 gaps + US2 player" scope).
- [~] T005 `Enrollment` exists with access windows/status/capacity enforcement. **`Waitlist` does NOT exist** (`docs/lms/DECISION_GATES.md` gates #20/#41: "deferred to Part 3") — not addressed this pass.
- [x] T006 `LessonProgress`, and course/module aggregate progress (`progress.service.ts`'s `computeModuleProgress`/`computeCourseProgress`) exist. **No separate `PathProgress`/`VideoProgress` entities** (no LearningPath to aggregate against; no video-streaming infra to telemetry) — correctly absent, not a gap, given T004's LearningPath status.
- [ ] T007 [P] No `LearnerNote`/`Bookmark` entities exist. Not addressed this pass.
- [x] T008 **(Quiz System batch, 2026-07-29)** `Quiz`/`Question`/`QuestionOption`/`QuizAttempt`/`QuizAnswer` — built (migration `20260728183309_spec004_quiz_system`); see Phase 5's revision note. This line was left stale when that batch landed — corrected here during the Certificate batch's re-audit.
- [x] T009 **(Assignment System batch, 2026-07-29)** `Assignment`/`RubricCriterion`/`Submission`/`SubmissionCriterionScore` — built (migration `20260729101324_spec004_assignment_system`); see Phase 6's revision note. `SubmissionFile`/`Review` do not exist — submissions are link-based (no upload/storage pipeline), a documented scope reduction, not an oversight. Corrected here during the Certificate batch's re-audit.
- [x] T010 [P] **(Certificate System batch, 2026-07-29)** `CertificateTemplate`/`Certificate` — built (migration `20260729104604_spec004_certificate_system`); see Phase 7's revision note.
- [ ] T011 [P] No `LiveSession`/`Attendance` entities exist. Not addressed this pass (no live-class provider integration in scope).
- [x] T012 [P] `CourseReview` (Discovery & Recommendations batch, Phase 7b) and `LearningEvent` (**this batch**, Learning Analytics & At-Risk Detection — migration `20260729094813_spec004_learning_analytics`) both now exist. This line was stale (left unmarked after the Discovery batch landed `CourseReview`) — corrected here during the Learning Analytics batch's own re-audit, same "correct a stale line during the next batch's audit" pattern T008/T009 already established. **`Announcement` still does not exist** — no owning FR names it yet; genuinely not addressed.
- [x] T013 `backend/src/lms/completion.service.ts`'s `getEffectiveCompletionRules`/`evaluateAutomaticRules` — pre-existing, re-verified this pass as genuinely solid: AND-combined multi-condition support, and `areAllRequiredActivitiesViewed` is always server-derived from real `ActivityProgress` rows, never a client-asserted boolean.
- [x] T014 `backend/src/lms/access-evaluator.service.ts`'s `isModuleReleased` (drip) + `module.service.ts`'s `assertNoModulePrerequisiteCycle` (circular-dependency detection at save time) — pre-existing, reused as-is by this pass's new `curriculum.service.ts`. **Module-level only** — no per-lesson drip (`docs/lms/DECISION_GATES.md` gate #22, a documented scope boundary, not an oversight).
- [x] T015 `backend/src/lms/course-lifecycle.policy.ts` — pre-existing, enforces the full Draft→Submitted→Changes-requested→Approved→Scheduled→Published chain with publish/edit permission separation (verified via `docs/lms/RBAC.md`).
- [ ] T016 No dedicated `lms-error-codes.ts` taxonomy file exists — errors use the generic shared `AppError`/`ERROR_CODES` (e.g. `COURSE_FULL`, `COURSE_UNAVAILABLE` as string literals inline, not a centralized enum). Not addressed this pass.
- [ ] T017 No dedicated contract test file at this path — the no-vanity-completion guarantee is covered indirectly by existing LMS integration tests (`lms.integration.test.ts`, `lms-part2.integration.test.ts`) rather than a standalone contract test. Not addressed this pass.
- [ ] T018 No dedicated contract test file at this path — circular-dependency rejection is covered by an existing integration test ("rejects a module prerequisite cycle" in `lms.integration.test.ts`). Not addressed this pass.
- [ ] T019 No dedicated contract test file at this path — per-request entitlement verification is covered throughout `lms-part2.integration.test.ts`'s access-control scenarios rather than a standalone contract test. Not addressed this pass.

**Checkpoint**: Foundation ready — user story implementation can begin.

---

## Phase 3: User Story 1 — Enroll and Gain Access via an Entitlement Type (P1) 🎯 MVP

**Independent Test**: Enroll a test learner under each entitlement source (free, membership, purchase, org-assigned, coupon); confirm lesson content is served only after server-side entitlement verification, with correct failure reasons for unsupported cases.

- [x] T020 [US1] `backend/src/lms/enrollment.service.ts`'s `createEnrollmentInternal`/`selfEnroll` — pre-existing, full orchestration (entitlement→capacity→create→audit), tested end-to-end in `lms-part2.integration.test.ts`.
- [~] T021 [US1] Denial reasons exist for the sources that actually work (`COURSE_FULL`, `COURSE_UNAVAILABLE`, entitlement-`UNAVAILABLE`) — membership/payment/region/org-license-specific reasons don't apply since those entitlement sources are deliberately fail-closed (Volume 09 not built). Not a gap for the sources this codebase actually supports.
- [x] T022 [US1] `enrollment.policy.ts`'s `isEnrollmentAccessWindowOpen` + `access-evaluator.service.ts`'s `ACCESS_NOT_STARTED`/`ACCESS_EXPIRED` — pre-existing, server-side, timestamp-based (no background job dependency).
- [~] T023 [US1] Capacity rejection (`COURSE_FULL`) exists and is enforced. **Waitlist does NOT exist** (see T005) — join/time-limited-offer/next-priority-pass-through not addressed this pass.
- [ ] T024 [P] [US1] No Wishlist entity/service exists. Not addressed this pass.
- [ ] T025 [US1] No refund-driven access policy exists — correctly absent, since there is no Order/Payment/Refund system yet (Volume 09) to trigger it from.
- [ ] T026 [P] [US1] No bulk CSV import exists. Not addressed this pass.
- [x] T027 [US1] **(this session)** `backend/src/lms/org-assignment.service.ts` — `assignCourseToOrganizationMembers` (bulk assign + per-user result report), `listOrganizationCourseEnrollments` (tracking/reporting), `removeOrganizationMemberAccess` (remove access), `setOrganizationMemberDeadline` (deadlines). Cohort/invite are correctly out of scope (no Cohort entity — see T004). Every function re-verifies BOTH actor's and target's `organizationId` before acting — never reaches a learner's notes/unrelated data (no such endpoint exists here at all). Verified via `backend/tests/integration/org-assignment-lesson-player.integration.test.ts` (cross-org denial + same-org success) and a live curl walkthrough against the running dev server.
- [x] T028 [P] [US1] **(this session)** `frontend/src/pages/CourseDetailPage.tsx` — real enroll CTA calling `POST /lms/me/enrollments`, driven by the server's own `GET /lms/me/courses/:courseId/access` decision (not a client guess). No waitlist-join UI (see T023/T024).
- [ ] T029 [US1] No dedicated integration test file at this path — enrollment is covered across `lms-part2.integration.test.ts` (pre-existing) and this pass's new `org-assignment-lesson-player.integration.test.ts`. Not addressed as a single consolidated file this pass.

**Checkpoint**: Enrollment/entitlement gate independently functional — the P0 prerequisite for every other LMS capability.

---

## Phase 4: User Story 2 — Consume Lessons and Track Multi-Condition Completion (P1)

**Independent Test**: Configure a multi-condition lesson, consume it partially (remains incomplete), then satisfy all conditions and confirm completion syncs across a second device.

- [~] T030 [US2] **(this session)** `frontend/src/pages/LessonPlayerPage.tsx`'s `LessonActivity` renders a real HTML5 `<video>` with play/pause/seek/volume/speed/fullscreen (native browser controls) and resume (via `lastPosition` sent to the server). **No PiP toggle, no quality-selector UI, no per-video telemetry service** — `LearningActivity` has no video-specific telemetry table (see T004/T006); watch-percent is derived from `currentTime/duration` client-side and reported via the existing `updateLessonProgress` endpoint, not a dedicated video-progress service.
- [x] T031 [US2] `completion.service.ts`'s `evaluateAutomaticRules` (pre-existing) — `MINIMUM_WATCH_PERCENT` defaults to 80% (`lesson.completionRuleValue.minPercent ?? 80`), confirmed by reading the code this pass.
- [ ] T032 [US2] No signed-URL video delivery exists — `LearningActivity.mediaUrl` is a plain URL (`docs/lms/DECISION_GATES.md` gate #31: "no file-hosting/upload pipeline exists"). Not addressed this pass — would require a real media/CDN provider decision (T002).
- [ ] T033 [P] [US2] No captions/transcript exist. Not addressed this pass.
- [x] T034 [P] [US2] **(this session)** `LessonPlayerPage.tsx`'s `LessonActivity` renders a real `<audio>` element (play/pause/seek/speed/resume via native controls + `lastPosition`). No background-playback or sleep-timer (both are mobile-app concepts; no mobile app exists in this repo).
- [x] T035 [P] [US2] **(this session)** `LessonActivity`'s `ARTICLE` case renders sanitized rich-text (`sanitizeRichText`, the same DOMPurify allowlist the CMS/course-description paths already use) with a scroll/click-based "viewed" signal. No TOC/reading-time/translation-variant UI.
- [x] T036 [P] [US2] **(this session)** `LessonActivity`'s `PDF` case renders an in-app `<iframe>` viewer with a viewed-on-load signal. No page navigation/search/zoom/bookmark/resume-page (an iframe-embedded browser PDF viewer, not a custom in-app one), no watermarking, no access-logging beyond the existing generic `activities.viewed` signal.
- [ ] T037 [P] [US2] No dedicated downloadable-resource catalog service exists — `DOWNLOAD`-type activities (rendered this pass in `LessonActivity`) use the same generic `LearningActivity` fields as every other type, not a richer resource-catalog model (version/permission/language fields). Not addressed this pass.
- [ ] T038 [US2] No Live Class lesson state machine or Google Meet/Zoom integration exists. Not addressed this pass.
- [x] T039 [US2] **(this session)** `LessonPlayerPage.tsx` — curriculum sidebar (desktop), activity content area, prev/next navigation, mark-complete. Single responsive layout, not a distinct mobile content-first variant; no dedicated notes/resources/discussion panels (none of those features exist — see T007/T045).
- [ ] T040 [US2] No offline queue/conflict-resolution sync exists — correctly out of scope, since no mobile app exists in this repo to go offline from. Progress updates are synchronous and server-authoritative (`updateLessonProgress`), which satisfies the "server-authoritative" half of FR-056 but not the offline-queue half.
- [x] T041 [US2] `progress.service.ts`'s `computeModuleProgress`/`computeCourseProgress` (pre-existing) — mandatory-only weighting, optional lessons don't block completion; **extended this session** to also return a per-lesson `status` array, now powering the lesson-player sidebar's checkmarks.
- [ ] T042 [P] [US2] No Learning Streak exists. Not addressed this pass.
- [ ] T043 [P] [US2] No Learner Notes exist. Not addressed this pass.
- [ ] T044 [P] [US2] No Bookmarks exist. Not addressed this pass.
- [ ] T045 [P] [US2] No per-lesson discussion exists (no Community infrastructure from `005` exists yet to build it on). Not addressed this pass.
- [ ] T046 [US2] No mobile app exists in this repo at all. Not applicable/not attempted.
- [ ] T047 [US2] Same as T046 — not applicable (web-only monorepo).
- [ ] T048 [US2] Same as T046 — not applicable.
- [x] T049 [US2] **(this session)** `backend/tests/integration/org-assignment-lesson-player.integration.test.ts`'s "Course curriculum for the lesson player" suite — covers pre-enrollment 403, post-enrollment unlocked+`NOT_STARTED`, and post-completion `COMPLETED` status reflected in the curriculum endpoint. No offline-sync test (not applicable — see T040/T046).

**Checkpoint**: Core day-to-day learning loop independently functional.

---

## Phase 5: User Story 3 — Attempt a Quiz or Assessment (P1)

**Independent Test**: Configure a graded quiz with a passing score and two allowed attempts; take it once to fail and once to pass; confirm result screen, retake eligibility, and downstream unlocks reflect correct state.

**Revision note (2026-07-29, Quiz System batch)**: The prior revision note above marked US3 "entirely unbuilt, explicitly deferred" as one narrower implementation batch's scope decision — the user explicitly corrected that framing: a scope-limited batch is not spec completion, and Spec 004 continues in further batches (Assignment System, Certificate System, LMS Admin UI, remaining quality tasks are still outstanding — see the batch report). This batch implemented the Quiz System end-to-end: `Quiz`/`Question`/`QuestionOption`/`QuizAttempt`/`QuizAnswer` models (migration `20260728183309_spec004_quiz_system`), a `QUIZ_PASS` lesson-completion rule wired into the pre-existing `completion.service.ts` engine, full admin CRUD, the learner attempt flow with server-side grading across 5 question types, attempt-limit enforcement, timer-expiry auto-submit, historical-attempt integrity after question archival, idempotent submission, member-facing quiz-taking UI, and a minimal admin quiz-management UI. Deliberately narrower than spec.md's fullest FR-064 vision (see file header note): questions belong to one quiz directly, not a separate reusable cross-quiz Question Bank with tagging/randomized-set-generation — a genuine, real, future enhancement, not silently dropped.

- [x] T050 [US3] `backend/src/lms/quiz-attempt.service.ts`'s `startOrResumeAttempt`/`saveAnswer`/`submitAttempt` — instructions (quiz.instructions field) → eligibility check (attempt-limit) → timer (expiresAt) → autosave (per-answer upsert) → submit-confirm → server grades → result → pass/fail → progress update (QUIZ_PASS trigger) → retake rules (maxAttempts error detail). All verified via `backend/tests/integration/quiz-system.integration.test.ts` (11 tests) and a live curl walkthrough against the running dev server.
- [x] T051 [US3] `quiz-attempt.service.ts`'s `autoSubmitIfExpired`/`isExpired` — a timer-expired IN_PROGRESS attempt is auto-graded on the next read/write touch (verified by test "auto-submits and scores a timer-expired attempt rather than losing it").
- [x] T052 [US3] `startOrResumeAttempt`'s `maxAttempts` check throws `QUIZ_ATTEMPT_LIMIT_REACHED` (409) with `{maxAttempts, attemptsUsed}` detail once finalized attempts reach the configured limit (verified by test).
- [~] T053 [P] [US3] Questions belong directly to one quiz (`quiz.repository.ts`) — no separate, cross-quiz-reusable Question Bank with category/difficulty tagging or randomized-set generation by distribution rules. A deliberate, documented scope reduction (see revision note above), not an oversight.
- [x] T054 [US3] `gradeAndFinalizeAttempt`'s question set is the union of currently-PUBLISHED questions and whatever question each recorded `QuizAnswer` actually points at (via `findQuestionByIdIncludingDeleted`, since `Question.deletedAt` soft-delete, never hard-delete) — a question archived mid-attempt still grades correctly and still counts toward `pointsPossible`. This exact bug (the naive `findQuestionsByQuiz(id, true)` approach silently drops soft-deleted questions regardless of its `includeArchived` flag) was caught and fixed by the dedicated integration test for this scenario before this task was marked done.
- [x] T055 [US3] `submitAttempt` wraps `beginIdempotentOperation` (same shared idempotency infrastructure as lesson-completion/enrollment) — resubmitting an already-GRADED attempt replays the identical result, never re-grades (verified by test).
- [x] T056 [P] [US3] `frontend/src/pages/QuizAttemptPage.tsx`'s graded-review branch — score, pass/fail, points, per-question correct/incorrect, admin-controlled visibility via `quiz.showCorrectAnswers`/`reviewVisible`. No separate topic-level performance breakdown.
- [ ] T057 [P] [US3] No broader Assessment types (self-assessment/skill-rating/scenario-task/practical-project/viva/portfolio-review) — out of scope this batch, a real Quiz engine was the priority.
- [x] T058 [US3] `frontend/src/pages/QuizAttemptPage.tsx` — single-choice/multiple-choice/true-false/short-answer/fill-blank/numeric question rendering with autosave. No match/ordering/file-upload/scenario/audio/video-response types (explicitly future-ready, matching this codebase's established "don't preclude, don't fake" pattern for out-of-scope question types).
- [x] T059 [US3] `backend/tests/integration/quiz-system.integration.test.ts` — 11 tests covering: zero-question publish rejection, 1:1 lesson-quiz constraint, non-enrolled access denial, full-credit grading across all 5 question types with pre-grading answer-key concealment verified, zero-credit grading with unanswered questions counted incorrect, attempt resume, attempt-limit block, timer-expiry auto-submit, historical-attempt integrity after question archival, idempotent resubmission, and QUIZ_PASS lesson-completion integration. Plus 3 frontend tests (`QuizAttemptPage.test.tsx`). No dedicated network-interruption-dedup test beyond the idempotency test (same underlying mechanism).

**Checkpoint**: Assessment loop independently functional.

---

## Phase 6: User Story 4 — Submit an Assignment and Receive Rubric-Graded Feedback (P1)

**Independent Test**: Submit a file-upload assignment; instructor scores against rubric and requests changes; learner resubmits; instructor approves; learner is notified with rubric feedback visible.

**Revision note (2026-07-29, Assignment System batch)**: This batch implemented the Assignment System end-to-end: `Assignment`/`RubricCriterion`/`Submission`/`SubmissionCriterionScore` models (migration `20260729101324_spec004_assignment_system`), an `ASSIGNMENT_APPROVED` lesson-completion rule wired into the same `completion.service.ts` engine Quiz's `QUIZ_PASS` uses, full admin CRUD (assignment + rubric criteria), the learner submission flow (draft/save/submit with late-policy enforcement), attempt-limit-gated resubmission after CHANGES_REQUESTED that preserves every prior attempt's history, instructor rubric-scoring review with APPROVE/REQUEST_CHANGES/REJECT decisions, member-facing submission UI, and admin review UI. Rubric criteria belong directly to one assignment (no separate, cross-assignment-reusable Rubric template entity) and file-shaped submission formats are link-based (no upload/storage pipeline exists in this codebase) — both deliberate, documented scope reductions mirroring Quiz's own. Same historical-integrity class of bug Quiz's Question-archival test caught was found and fixed here too (a scored `RubricCriterion` archived after review must still resolve its title in the submission-review display) — caught by this batch's own dedicated integration test before being marked done.

- [x] T060 [US4] `backend/src/lms/assignment-submission.service.ts`'s `submitSubmission` — orchestrates open (assignment metadata surfaced on `GET /me/lessons/:id`) → instructions/rubric (`GET /admin/assignments/:id` for rubric, learner sees it via the lesson payload) → save draft (`saveDraft`) → required-fields check (at least `textBody` or `linkUrl`) → submit confirm → lock-or-edit (DRAFT-only editing) → status update. No separate "notify reviewer"/"notify learner" step — no notification system exists anywhere in this codebase yet (a pre-existing, cross-cutting gap, not unique to this batch).
- [x] T061 [US4] `assignment-submission.service.ts`'s `submitSubmission` late-policy check — `latePolicy: REJECT` blocks a past-due submission (400) at the assignment's `dueAt`; `ACCEPT` allows it with `isLate: true`. Verified via `backend/tests/integration/assignment-system.integration.test.ts`.
- [x] T062 [US4] `admin/src/pages/SubmissionReviewPage.tsx` + `reviewSubmission` service — learner details, submission content, rubric scoring per criterion with a private reviewer note vs. learner-facing feedback field, and approve/request-changes/reject actions. No "previous attempts" panel in the review UI itself (the learner-facing history exists via `getMySubmissionHistory`; the admin review screen shows the one submission being reviewed) — a real, minor gap, not a fabricated feature.
- [x] T063 [US4] `RubricCriterion` model + `assignment.service.ts`'s `addRubricCriterion`/`updateExistingCriterion`/`archiveRubricCriterion` — criteria, description, weight-as-`maxPoints`, per-criterion reviewer comment (`SubmissionCriterionScore.comment`). No separate "performance levels" (rubric levels like Excellent/Good/Needs-Work per criterion) — a numeric point award only, a real scope reduction.
- [x] T064 [US4] `SubmissionStatus` enum (DRAFT/SUBMITTED/UNDER_REVIEW/CHANGES_REQUESTED/APPROVED/REJECTED/EXCUSED) — "Not started" has no row by design (FR-072's own framing); "Overdue"/"Late submitted" are derived (`isLate` flag + `dueAt` comparison), not separate stored statuses. Resubmission after CHANGES_REQUESTED creates a NEW `Submission` row (never overwrites), verified by the dedicated "preserves the previous submission" integration test.
- [x] T065 [US4] Covered by the same `submitSubmission` late-policy logic as T061 (FR-070's "late policy" and FR-072's "Late submitted" flagging are the same mechanism in this implementation, not two separate systems).
- [x] T066 [US4] `completion.service.ts`'s `maybeAutoCompleteFromAssignmentApproval`, called from `reviewSubmission` only on an APPROVE decision — verified end-to-end (submit → approve → lesson `COMPLETED`) both in the integration test suite and a live curl walkthrough against the running dev server.
- [ ] T067 [P] [US4] No dedicated instructor-feedback-format service exists — feedback is a single `learnerFeedback` text field (+ per-criterion `comment`). No audio/video/annotated-file feedback, no "mark viewed"/"reply"/"request clarification" learner actions beyond re-reading the feedback text on the assignment page. Not addressed this pass (no media pipeline exists to build audio/video feedback on).
- [ ] T068 [P] [US4] No project-based "multiple required-artifact submissions" concept exists — one assignment has one submission stream. Not addressed this pass.
- [x] T069 [US4] **(this session)** `frontend/src/pages/AssignmentPage.tsx` — draft editor (text + optional link), save/submit actions, status display, reviewer feedback display, resubmit action after CHANGES_REQUESTED, and a visible attempt-history list. This is the frontend half of FR-071's submission flow.
- [x] T070 [US4] `backend/tests/integration/assignment-system.integration.test.ts` — 9 tests covering: 1:1 lesson-assignment constraint, non-enrolled access denial, full draft→submit→approve cycle with correct rubric-sum scoring and ASSIGNMENT_APPROVED completion, late-flagging under both late policies (ACCEPT/REJECT), resubmission-preserves-history + attempt-limit enforcement, historical-criterion integrity after archival, idempotent resubmission, and REJECT-decision handling. Plus 3 frontend tests (`AssignmentPage.test.tsx`). No dedicated "unsupported file type" rejection test — file-shaped submissions are link-based in this implementation (see revision note), so there is no file-type validation surface to test.

**Checkpoint**: The primary "action, not consumption" mechanism is independently functional.

---

## Phase 7: User Story 5 — Earn and Publicly Verify a Certificate (P1)

**Independent Test**: Bring a test learner to 100% eligibility; confirm certificate generation with a unique verification code; visit the public verification URL as an anonymous, logged-out user and confirm correct status and minimal data.

**Revision note (2026-07-29, Certificate System batch)**: The line above marked US5 "entirely unbuilt, explicitly deferred" as of the Quiz/Assignment batches — this batch implemented the Certificate System end-to-end: `CertificateTemplate`/`Certificate` models (migration `20260729104604_spec004_certificate_system`), an FR-081 server-evaluated eligibility snapshot reusing real existing signals (`Enrollment.status === 'COMPLETED'` from the pre-existing completion engine, `User.emailVerifiedAt`, `User.status`, and the pre-existing-but-previously-unconsumed `Course.certificateAvailable` flag), idempotent FR-083 generation with historically-immutable snapshotted learner/course/instructor names (never a live join), FR-082/FR-084 template CRUD + course-template mapping, FR-085 public no-auth verification (credential ID lookup, `NOT_FOUND` as a response value not an HTTP error), FR-086 revocation (permanent, queryable-forever record, no hard delete), member-facing "My Certificates"/certificate-view/public-verify pages, and admin template-management/course-certificate-list/revoke UI. Deliberately narrower than spec.md's fullest FR-081 vision (see schema doc comments): of the 8 named eligibility conditions, only 4 map to real signals this codebase can verify (certificate-available flag, enrollment completion, email-verified-as-identity-proxy, account-good-standing-as-misconduct-proxy) — attendance threshold, final-project approval, and payment-settled have no owning system yet (no Live Session/project-based-learning/Volume-009 payment system) and are honestly reported via a `notApplicable` list rather than assumed satisfied. "PDF" (FR-083) is a server-rendered, browser-printable HTML view (no PDF library/file-storage pipeline exists in this codebase) — the public verification page doubles as the FR-085 deliverable. QR-code image rendering is deferred (credential ID + verification URL is the fully-functional verification mechanism regardless).

- [x] T071 [US5] `database/prisma/schema.prisma`'s `Certificate`/`CertificateTemplate`/`CertificateType`/`CertificateStatus` — migration `20260729104604_spec004_certificate_system`, applied to both `coachx_dev` and `coachx_test`. `eligibilitySnapshot` (Json) captures exactly which conditions were checked and their result at issuance time (SC-002: "100% of certificates issued have a system-recorded, server-evaluated eligibility snapshot").
- [x] T072 [US5] `backend/src/lms/certificate.service.ts`'s `evaluateCertificateEligibility` — real signals only (certificateAvailable flag, enrollment completion, email verification, account status); `notApplicable` honestly names the 3 FR-081 conditions this codebase cannot verify. Verified via `backend/tests/integration/certificate-system.integration.test.ts` and a live curl walkthrough against the running dev server.
- [x] T073 [US5] `certificate.service.ts`'s `generateCertificateForEnrollment` — idempotent (returns the existing `VALID` certificate for a re-request, never a duplicate row), rejects with the failing conditions surfaced when ineligible, snapshots learner/course/instructor names and completion date rather than joining live (Historical Immutability, Constitution Article IV), generates a short human-shareable `CX-XXXXXXXXXX` credential ID with a uniqueness-retry loop.
- [x] T074 [US5] `certificate.service.ts`'s template CRUD (`createCertificateTemplate`/`updateExistingTemplate`) + `mapCourseTemplate` — course-to-template mapping never retroactively re-templates an already-issued certificate. `admin/src/pages/CertificateTemplatesPage.tsx` — create/list/activate-deactivate + course-mapping UI.
- [x] T075 [US5] `certificate-verification.service.ts`'s `verifyCertificateByCredentialId` — public, no-auth, minimal-data FR-085 response; `NOT_FOUND` is a response value (200 OK), never a 404, so a mistyped/forged credential ID can't be used to probe for lookup-layer behavior. `frontend/src/pages/CertificateVerifyPage.tsx` — public `/verify/:credentialId` route, mounted outside `RequireAuth`.
- [x] T076 [US5] `certificate-verification.service.ts`'s `revokeCertificate` — permanent status change (never a hard delete, stays queryable/verifiable forever as `REVOKED`), rejects a second revoke of an already-revoked certificate with 409. `admin/src/pages/CourseCertificatesPage.tsx` — per-course issued-certificate list + revoke-with-reason UI.
- [x] T077 [US5] `backend/tests/integration/certificate-system.integration.test.ts` — 11 tests covering: pre/post-completion eligibility detail, certificate-unavailable-course rejection, generation-while-ineligible rejection with surfaced conditions, historically-immutable snapshotting, idempotent generation (no duplicate), ownership-scoped certificate detail (no IDOR), template CRUD + course-mapping + templateId propagation onto issued certificates, template deactivation without deletion, public verification (valid + not-found), and revocation (+ 409 on double-revoke). Plus 7 frontend tests (`CertificatesPage.test.tsx`, `CertificateViewPage.test.tsx`, `CertificateVerifyPage.test.tsx`). All verified live against `coachx_dev` via a full curl walkthrough (register → verify → enroll → complete → eligibility → generate → idempotent re-generate → list → detail → public verify → template create/map → admin list → revoke → verify-after-revoke → double-revoke 409).

**Checkpoint**: The platform's core credibility deliverable is independently functional — all 5 P1 stories complete.

---

## Phase 7b: Discovery & Recommendations (supports FR-087–FR-090; cross-cutting, no single owning story)

**Revision note (2026-07-29, Discovery & Recommendations batch)**: The line below previously marked this whole phase "not addressed — no Course Review model, no Recommendation Engine, no cross-content search." This batch built all four FRs to the extent this codebase's real, already-built signals support:
- **FR-087 Course Reviews**: `CourseReview` model (migration `20260729062654_spec004_discovery_recommendations`) + `course-review.service.ts` — eligibility (enrolled AND (completed OR ≥50% progress — a documented default threshold, same "instructor-configurable, else a documented default" pattern as `MINIMUM_WATCH_PERCENT`'s 80%)), one review per user per course enforced as an upsert (never a duplicate, never silently rejected), admin moderation as a HIDE/RESTORE status flip (never a delete — an instructor holding only authoring permissions has no path to this endpoint at all), and a real rating aggregate recompute on every write that finally populates `Course.ratingAverage`/`ratingCount` — placeholder fields the Part 1 schema doc comment explicitly reserved for "Part 3 (Course Review)." Learner-facing review UI on `CourseDetailPage.tsx`; admin moderation on `admin/src/pages/CourseReviewsPage.tsx`.
- **FR-088 Recommendation Engine**: `recommendation.service.ts` — a genuinely deterministic engine (not a "fallback" bolted onto a real AI path, since no AI wrapper exists anywhere in this codebase yet — 008 is a later, not-yet-reached spec). Three of six named output types have a real owning signal and are implemented: next course (same-category popularity, using real `learnerCount`), revision lesson (the lesson behind a learner's most recent failed graded quiz attempt), and practice quiz (a retake offer, gated on real `maxAttempts`/attempts-used data). The other three (mentor support, related resource, challenge) have no owning entity yet (specs 007/006, no standalone Resource model) and are honestly reported via `notApplicable`, never guessed at. Wired into the **existing** `dashboard.service.ts` "recommendations" widget stub (previously a permanent `emptyWidget('Personalized recommendations are not available yet.')` placeholder from spec 003) — a real integration point that spec anticipated, not new scope creep.
- **FR-089 extended learning search**: `course.repository.ts`'s `publicCourseWhere` now also matches instructor display name and published lesson titles (so a search for an instructor or a specific lesson topic surfaces the containing course), plus new `certificateAvailable`/`maxDurationMinutes`/`format` filters and a `popular` (learnerCount-desc) sort. "Progress state" filtering is deliberately NOT added to the anonymous public search (it requires a learner identity) — it lives in the catalog's personalized sections instead.
- **FR-090 member catalog view**: `catalog.service.ts`'s `getMemberCatalog()` + `GET /me/catalog`, surfaced on the new `frontend/src/pages/CatalogPage.tsx`. Six real sections (continueLearning, recommended, newCourses, popular, free, completed) each reflect actual enrollment/discovery data; card states (Start/Continue/Completed/Locked/Coming soon) are derived from real enrollment status and course pricing — `Upgrade`/`Expired` card states are not modeled (no membership-gated content path exists to test either against). The three sections FR-090 names that have no owning entity (learningPaths, wishlist, includedInMembership) are honestly reported unavailable via the same section-status shape, never fabricated.

8 backend integration tests + 4 new frontend tests (2 CourseDetailPage review tests, CatalogPage, CourseReviewsPage admin), all passing; full quality gate clean across backend/frontend/admin; every flow (review submit/moderate/rating-aggregate, recommendations, catalog sections, extended search) verified live end-to-end against `coachx_dev`.

- [x] T078 [P] **(this batch)** `course-review.service.ts` + `CourseReview` model — FR-087.
- [x] T079 **(this batch)** `recommendation.service.ts` — FR-088's deterministic baseline (3 of 6 output types real; 3 honestly `notApplicable`), wired into the dashboard's pre-existing widget stub.
- [x] T080 **(this batch)** `course.repository.ts`'s extended `publicCourseWhere` + `lms.validation.ts`'s new filters/sort — FR-089.
- [x] T081 **(this batch)** `catalog.service.ts` + `GET /me/catalog` + `CatalogPage.tsx` — FR-090.

**Checkpoint**: Discovery and recommendation surfaces independently functional for every FR-087–090 signal this codebase can genuinely compute; mentor-support/related-resource/challenge recommendations, AI-driven personalization, and membership-gated catalog states remain blocked on later specs (007/006/008/009) or entities that don't exist yet (standalone Resource, LearningPath, Wishlist).

---

## Phase 8: User Story 6 — Progress Through Drip-Released, Prerequisite-Gated Content (P2)

**Independent Test**: Configure a module to unlock 3 days after enrollment and a second module gated on the first module's assessment; confirm a learner cannot access either early, sees the correct unlock condition/countdown, and gains access exactly on schedule.

**Revision note (2026-07-29, US6 polish batch)**: T082/T086/T087 below previously named real, small gaps in an otherwise-functional US6 — this batch closed all three, plus built a genuine (if intentionally bounded) answer to T084. `Course.sequencingMode` (migration `20260729091908_spec004_us6_sequencing_mode`) is a course-wide AUTHORING-TIME default: SEQUENTIAL auto-chains each newly-created module after the previous one (`releaseRuleType: AFTER_PREVIOUS_MODULE` + `prerequisiteModuleId` set automatically), INSTRUCTOR_CONTROLLED defaults new modules to manual release, FLEXIBLE/HYBRID apply no default (byte-for-byte the same as this codebase's pre-existing behavior, so every already-created course is completely unaffected). An admin's explicit `releaseRuleType`/`prerequisiteModuleId` in the request always wins over the derived default — verified by a dedicated test and live against `coachx_dev`. T085 (Cohort) is explicitly NOT part of this batch — building a real Cohort entity (roster, capacity, cohort-scoped release evaluation) is a genuinely new system, not "polish," and remains a separate future effort.
- [x] T082 [US6] **(this batch)** `LessonPlayerPage.tsx`'s curriculum sidebar now shows a real countdown ("Unlocks in 3 days" / a formatted date for far-future unlocks) or a human-readable reason ("Complete the previous module to unlock") instead of a bare 🔒 icon, using `curriculum.service.ts`'s existing `unlockAt`/`lockReason` fields — no backend change needed, this was purely a frontend gap.
- [x] T083 [US6] `access-evaluator.service.ts`'s `evaluateModuleAccess`/`evaluateLessonAccess` (pre-existing, re-verified) — server-side denial is independent of any UI state.
- [~] T084 [US6] `Course.sequencingMode` + `module.service.ts`'s `deriveModuleSequencingDefaults` — a real, bounded answer: an authoring-time default for NEW modules, not a runtime enforcement engine distinct from the existing per-module `releaseRuleType`/`prerequisiteModuleId` mechanism (which remains the sole enforcement path). This is a deliberate scope match to "sequencing MODE" as a naming/defaulting convenience — a fully independent Sequential/Flexible/Hybrid/Instructor-Controlled state machine with its own enforcement semantics was not built, since the existing per-module mechanism already IS the enforcement engine these modes configure.
- [ ] T085 [US6] **Explicitly deferred, not "polish"** — no Cohort entity exists; cohort-scheduled simultaneous release needs a real new entity (roster, capacity, cohort-scoped release-rule evaluation), out of scope for this batch.
- [x] T086 [US6] **(this batch)** `ModuleEditorPage.tsx` — a prerequisite-module `<select>`, filtered to only earlier-position sibling modules (matching the server's own ordering rule, so the UI never offers a choice the backend would reject).
- [x] T087 [US6] **(this batch)** `backend/tests/integration/drip-prerequisite.integration.test.ts` — 9 tests: sequencing-mode-derived defaults (all 4 modes + explicit-override-always-wins), prerequisite validation (later-position rejection, cross-course rejection, self-prerequisite rejection), and end-to-end learner-facing denial-then-unlock via the curriculum endpoint.

**Checkpoint**: Structured, non-random progression independently functional, including course-wide sequencing-mode authoring convenience and full admin prerequisite configuration; Cohort-based simultaneous release remains a separate future effort.

---

## Phase 9: User Story 7 — Build and Publish a Course via the Course Builder (P2)

**Independent Test**: An instructor without publish rights builds a complete course through all ten builder steps, submits for review, a reviewer requests changes, the instructor resolves them and gets approval, and only a publish-permission holder can move it to Published.

- [x] T090 [US7] `course-lifecycle.policy.ts`'s workflow enforcement (pre-existing, re-verified — see T015) — publish permission separate from edit permission, every transition audited.
- [x] T091 [US7] `isCourseVisibleByDirectLink`/`evaluateCourseAccess` (pre-existing) already restrict Draft content to admins/assigned instructors.

**Revision note (2026-07-29, LMS Admin UI batch)**: The line below previously marked the entire admin frontend "not addressed — no admin frontend at all." This batch built it: `admin/src/pages/LmsCategoriesPage.tsx` (category CRUD), `LmsCoursesPage.tsx` (searchable/filterable course list + create), `CourseEditorPage.tsx` (metadata edit, the FR-100 status-workflow buttons driven by the exact `course-lifecycle.policy.ts` transition map so the UI never offers a transition the server would reject, instructor assignment, and a modules list), `ModuleEditorPage.tsx` (module metadata/release-rule/publish/release-now + its lessons list), `LessonEditorPage.tsx` (lesson metadata/completion-rule/publish + its activities list + links into this lesson's Quiz/Assignment editor), and `CourseEnrollmentsPage.tsx` (grant/suspend/reactivate/revoke/extend-access/override-complete/reset-progress). Every one of these reuses an already-built, already-tested backend `/lms/admin/*` endpoint (`admin-lms.controller.ts`) — no backend code changed this batch. Reordering (modules/lessons/activities) uses simple ↑/↓ buttons calling the existing reorder endpoints, not drag-and-drop (no new UI-library dependency added). All wired into the sidebar nav and verified live against `coachx_dev` end-to-end (category→course→status-transition→instructor→module→lesson→activity→enrollment-lifecycle, including reorder and archive/restore). 11 new frontend tests — plus the admin app's test infrastructure itself (`vitest`/`@testing-library/react`/`jsdom`) had to be added from scratch, since no admin test tooling existed before this batch (a real, pre-existing gap, not a regression).
Deliberately narrower than spec.md's fullest US7 vision: this is a flat multi-section editor, not a distinct ten-step guided wizard (the Independent Test's literal framing) — functionally equivalent CRUD coverage, not a wizard UX. Still missing: a `prerequisiteModuleId` picker in `ModuleEditorPage` (the field exists in the API type but no UI control was added — T086 remains a real gap), a course-version-history view (`GET /admin/courses/:id/versions` exists in the backend, unused by any admin page), course cloning (T104–T107, US8 — depends on this builder, not attempted this batch), translation/multi-language course variants, Announcements (no `Announcement` entity exists — T012), and a calendar/reminders view.
- [x] T088 [US7] `admin/src/pages/LmsCoursesPage.tsx` + `CourseEditorPage.tsx` — course list, create, and metadata-editing screens (the "builder" entry point).
- [x] T089 [US7] `admin/src/pages/ModuleEditorPage.tsx` + `LessonEditorPage.tsx` — module/lesson/activity authoring screens, reusing the pre-existing backend validation (`lms.validation.ts`/`lesson.validation.ts`) for every per-activity-type required-field rule.
- [ ] T092–T097 No distinct step-by-step wizard chrome, translation UI, or announcements/calendar/reminders screens — not addressed this pass (see revision note).
- [x] T098 [US7] `admin/src/components/layout/Sidebar.tsx` — "LMS Categories"/"LMS Courses" nav entries added (an admin LMS navigation section now exists, closing the "admin frontend has no LMS section at all" gap this line previously described).
- [x] T099 [US7] `admin/src/pages/LmsCoursesPage.tsx` — the admin course list (search by title, filter by status), previously entirely absent.
- [x] T100 [US7] `admin/src/pages/CourseEnrollmentsPage.tsx` — the admin enrollment-management screen (grant/suspend/reactivate/revoke/extend/override-complete/reset-progress), previously entirely absent.
- [ ] T101–T103 No dedicated LMS-wide settings screen (e.g. default certificate template, global thresholds) — not addressed this pass; every setting built so far is per-course/per-template (`CourseEditorPage`'s certificateAvailable checkbox, `CertificateTemplatesPage`'s course mapping).

**Checkpoint**: Content operations can scale beyond hand-seeded launch courses — the admin course-builder surface now exists end-to-end for course/module/lesson/activity/enrollment authoring; course cloning, translation, and LMS-wide settings remain open.

---

## Phase 10: User Story 8 — Clone an Existing Course for Reuse (P2)

**Independent Test**: Clone a published course with existing enrollments/progress using "curriculum only"; confirm the new course has the same modules/lessons but zero enrollments, zero progress, no financial linkage.

**Revision note (2026-07-29, Course Cloning batch)**: The line below previously marked US8 "not attempted — depends on Course-Builder infrastructure that also doesn't exist yet." The Course-Builder admin UI landed in the prior batch, so this batch built FR-098's cloning engine: `database/prisma/schema.prisma`'s `Course.translationOfCourseId` self-relation (migration `20260729061059_spec004_course_cloning`) and `backend/src/lms/course-clone.service.ts`'s `cloneCourse()`, exposed at `POST /admin/courses/:id/clone` and surfaced in `admin/src/pages/CourseEditorPage.tsx`'s new "Clone this course" section. Five of FR-098's six named modes are real and tested: **FULL** (everything content-side: modules/lessons/activities/quizzes+questions+options/assignments+criteria/instructors/certificate settings), **CURRICULUM_ONLY** (modules/lessons structure only — no activities/assessments/instructors/certificate settings), **CONTENT_WITHOUT_ENROLLMENTS** (same as FULL minus instructors — interpreted as "no staffing/assignment data carries over, only pure content," a defensible reading of an otherwise-ambiguous PRD name, documented in `course-clone.service.ts`), **CERTIFICATE_SETTINGS** (an otherwise-empty new course carrying only `certificateAvailable`/`certificateTemplateId`), and **TRANSLATION_VARIANT** (a FULL-equivalent copy plus the new `translationOfCourseId` link and a caller-chosen target language). Every mode resets the new course to an independent DRAFT with zero enrollments (never read, let alone copied, from the source), a remapped `prerequisiteModuleId` onto the new module ids (never left dangling or pointing at the source course), and pricing reset to FREE/0 (treated as "financial data" under SC-008's guarantee). **`ASSESSMENT_BANK` is the one deliberately unimplemented mode** — Quiz/Assignment attach 1:1 to a Lesson in this schema (the Quiz batch's own documented scope reduction: no cross-course-reusable Question Bank exists), so "clone only the assessment bank" has no coherent lesson-less target to copy questions/criteria onto; rather than fabricate synthetic empty-lesson containers, the mode is accepted by the type/enum (so the gap is discoverable) and rejected at the service layer with a clear explanation — both in a dedicated integration test and live against `coachx_dev`. 7 backend integration tests + 2 new frontend tests, all passing; full quality gate clean (prisma validate, typecheck/lint/test/build across backend + admin); every mode plus the ASSESSMENT_BANK rejection verified live end-to-end.
- [x] T104 [US8] `course-clone.service.ts`'s `cloneCourse()` — FULL/CURRICULUM_ONLY/CONTENT_WITHOUT_ENROLLMENTS/CERTIFICATE_SETTINGS/TRANSLATION_VARIANT modes, each mode's copy scope centralized in one `MODE_COPIES` table (curriculum/activities/assessments/instructors/certificateSettings booleans) rather than duplicated branching per mode.
- [x] T105 [US8] SC-008's "0% carry-over" guarantee — no `Enrollment`/`LessonProgress`/`QuizAttempt`/`Submission`/order-financial row is ever read by the clone service (verified: a source course with a real enrollment clones to zero enrollments, in both the integration test and the live `coachx_dev` walkthrough).
- [x] T106 [US8] `admin/src/pages/CourseEditorPage.tsx`'s "Clone this course" section — mode selector (with an inline warning + disabled submit for the unsupported ASSESSMENT_BANK mode), new slug/title/language inputs, navigates to the newly cloned course on success.
- [ ] T107 [US8] **Not addressed**: `ASSESSMENT_BANK` mode (see revision note) — a real, cross-course-reusable Question/Assignment Bank entity would need to exist first (the same future enhancement the Quiz/Assignment batches already deferred).

**Checkpoint**: Content-operations acceleration independently functional for 5 of 6 named clone modes; Assessment Bank remains blocked on a not-yet-built reusable Question Bank entity.

---

## Phase 10b: Learning Analytics & At-Risk Detection (supports FR-105–FR-109; cross-cutting, no single owning story)

**Revision note (2026-07-29, Learning Analytics & At-Risk Detection batch)**: The line below previously marked this whole phase "not addressed — no analytics collector, no admin analytics dashboards, no at-risk detection, no event-taxonomy emission." This batch built all five FRs to the extent this codebase's real, already-built signals support:
- **FR-109 event taxonomy**: `LearningEvent` model (migration `20260729094813_spec004_learning_analytics`, same "opaque reference, not a relation" design `AuditEvent` already established) + `learning-event.service.ts`'s `recordLearningEvent` (never throws, never blocks the real action that triggered it — same discipline `recordAuditEvent` established). FR-109 names 19 event types; 17 are real and wired into their exact real action point across `enrollment.service.ts` (COURSE_ENROLLED), `progress.service.ts` (COURSE_STARTED, VIDEO_STARTED/PROGRESSED), `completion.service.ts` (LESSON_COMPLETED, COURSE_COMPLETED, RESOURCE_DOWNLOADED), `student-lms.controller.ts`/`lms.controller.ts` (LESSON_VIEWED, COURSE_VIEWED — the latter gated behind the newly-added `authenticateOptional` on the public course-detail route, firing only for a resolvable logged-in viewer), `quiz-attempt.service.ts` (QUIZ_STARTED/SUBMITTED/PASSED/FAILED), `assignment-submission.service.ts` (ASSIGNMENT_STARTED/SUBMITTED/REVIEWED), and `certificate.service.ts` (CERTIFICATE_ISSUED). `learning_path_started`/`learning_path_completed` are deliberately NOT modeled — no `LearningPath` entity exists in this codebase (see T004) — modeling an event type for an entity that can never fire it would be a placeholder, not a real taxonomy member.
- **FR-105 user-level analytics**: `learner-analytics.service.ts`'s `getLearnerAnalyticsForEnrollment`, exposed at `GET /admin/enrollments/:id/analytics`. Ten of eleven named fields are real (enrollment/start/last-activity/progress/time-spent/lessons-completed/quiz-attempts+scores/assignment-status/certificate/drop-off, plus `atRiskScore` from FR-108); `attendance` has no owning entity (no Live Session model) and is honestly reported via `notApplicable` rather than fabricated.
- **FR-106 admin course analytics**: `course-analytics.service.ts`'s `getCourseAnalytics`, exposed at `GET /admin/courses/:id/analytics`. Nine of thirteen named fields are real (enrollments, activeLearners, completionRate, avgCompletionTimeDays, lessonDropOff — reusing FR-107's per-lesson analytics, quizPassRate, assignmentApprovalRate, rating — reusing `Course.ratingAverage`/`ratingCount` from the Discovery batch, certificateRate), plus a genuinely real `videoEngagement` metric (learners-with-video-activity + avg max-watched-percent, computed from this batch's own `VIDEO_STARTED`/`VIDEO_PROGRESSED` event log). `refundCorrelation` (no payment/refund system — spec 009), `deviceDistribution`, and `languageDistribution` (no device/session-language telemetry) have no owning signal and are honestly `notApplicable`.
- **FR-107 per-lesson analytics**: `lesson-analytics.service.ts`'s `getLessonAnalytics`/`getLessonAnalyticsForCourse`, exposed at `GET /admin/lessons/:lessonId/analytics` and `GET /admin/courses/:id/lessons/analytics`. Five of eleven named fields are real (views + uniqueLearners from the `LESSON_VIEWED` event log, starts/completes/avgTimeSpentSeconds from `LessonProgress`, resourceDownloads from the `RESOURCE_DOWNLOADED` event log); `dropOffTimestamp`/`replays` (no fine-grained video-scrubbing telemetry, only coarse watched-percent snapshots), `notesCreated` (no Notes entity — T043), `discussionActivity` (no Community/Discussion entity — spec 005), and `errorRate` (no client error-telemetry pipeline) are honestly `notApplicable`.
- **FR-108 at-risk detection**: `at-risk.service.ts`'s `assessAtRiskForEnrollment`/`listAtRiskLearnersForCourse`, exposed at `GET /admin/enrollments/:id/at-risk` and `GET /admin/courses/:id/at-risk-learners`. Five of six named signals are real, deterministic, and weighted into a 0–100 `atRiskScore` (no recent activity since enrollment past a grace period, long inactivity after starting, repeated graded quiz failure, missed past-due assignments, access nearing expiry) — `lowAttendance` has no owning signal (no Live Session/attendance entity) and is honestly `notApplicable`. Of FR-108's six named actions, two are real: `instructorAlertRaised` (the admin at-risk-learners list surfacing this enrollment IS the alert — same "the UI surface itself is the deferred-infra substitute" pattern Certificate's browser-printable page already established) and `recommendedRevision` (reuses `recommendation.service.ts`'s own failed-quiz→revision-lesson logic, scoped to one enrollment); `reminder` (no notification/email-delivery system for anything beyond the dev-console adapter), `mentorSuggestion` (no Mentor Marketplace — spec 007), `supportOutreach` (no support-ticket system), and `simplifiedRestartPlan` (no restart-plan generator) are honestly `notApplicable`.
- **Admin UI**: `admin/src/pages/CourseAnalyticsPage.tsx` (`/lms-courses/:id/analytics`, linked from `CourseEditorPage.tsx`) — course-level KPI cards, a lesson drop-off table, and the at-risk-learners list with per-signal chips, a recommended-revision callout, and an expandable drill-down into one learner's full FR-105 analytics.

8 backend integration tests (`learning-analytics.integration.test.ts`, covering the full FR-109 event lifecycle across enrollment/progress/quiz/assignment/certificate, FR-105/106/107 aggregate correctness against real multi-learner outcomes, and FR-108's repeated-quiz-failure detection plus real-elapsed-time signal detection via direct timestamp manipulation) + 2 new admin frontend tests (`CourseAnalyticsPage.test.tsx`); full quality gate clean across backend/frontend/admin (prisma validate, typecheck, lint, test, build); every FR-109 event type, the FR-105/106/107 aggregates, and FR-108's at-risk-learners list verified live end-to-end against `coachx_dev` (including a live-driven repeated-quiz-failure case surfacing correctly in the at-risk list).

- [x] T108 **(this batch)** `learning-event.service.ts` + `LearningEvent` model — FR-109's 17-of-19-real event taxonomy, wired into every real action point named above.
- [x] T109 **(this batch)** `learner-analytics.service.ts` — FR-105 single-learner analytics.
- [x] T110 **(this batch)** `course-analytics.service.ts` — FR-106 course-level analytics, including the real `videoEngagement` signal.
- [x] T111 **(this batch)** `lesson-analytics.service.ts` — FR-107 per-lesson analytics.
- [x] T112 **(this batch)** `at-risk.service.ts` — FR-108 at-risk detection (5 of 6 signals real, 2 of 6 actions real).
- [x] T113 **(this batch)** `admin/src/pages/CourseAnalyticsPage.tsx` — the admin analytics + at-risk-learners UI.

**Checkpoint**: Analytics and at-risk detection independently functional for every FR-105–109 signal this codebase's real, already-persisted data can support; attendance-based signals, notification-delivered actions (reminder/mentor-suggestion/support-outreach/restart-plan), device/language/refund analytics, and fine-grained video-scrubbing/discussion/notes analytics remain blocked on entities and platforms (Live Session, Mentor Marketplace, support-ticket system, Community, payment/refund system) that don't exist in this codebase yet.

---

## Phase 11: User Story 9 — Use AI-Assisted Learning and Peer Review Within Integrity Bounds (P3)

**Independent Test**: (a) Request an AI lesson summary and confirm it's presented as advisory, not authoritative; (b) submit an assignment for peer review, collect the configured reviews, and confirm the instructor can view/weigh/override the peer score.

- [ ] T114–T120 **US9 AI-Assisted Learning, Peer Review & Academic Integrity (P3) — not addressed this pass.** No AI wrapper, no peer-review workflow (depends on Assignment — see T060–T070), no academic-integrity investigation workflow exists. Not attempted (lowest priority tier, P3).

**Checkpoint**: All 9 user stories independently functional.

---

## Phase 12: Polish & Cross-Cutting Concerns

- [~] T123 [P] Security: server-side entitlement checks (pre-existing `access-evaluator.service.ts`, reused by this session's new endpoints), audit logs (`recordAuditEvent` on every org-assignment/quiz/assignment/certificate action), rate limiting (shared middleware) all real. **Updated (Certificate batch re-audit)**: quiz/submission/certificate protections now DO exist — ownership-scoped attempt/submission/certificate lookups (no IDOR, verified by dedicated tests in all three batches' integration suites), attempt/resubmission limits, idempotent submit/generate, server-evaluated eligibility never trusted from the client. No signed media/malware-scan (no upload pipeline exists) — that gap remains real.
- [ ] T121–T122, T124–T127 **Notification/email wiring, accessibility pass, performance pass, error-taxonomy verification, and full-`quickstart.md` validation — still not addressed.** Each of the Quiz/Assignment/Certificate batches validated only its own story via automated tests (Quiz: 11 backend + 3 frontend; Assignment: 9 backend + 3 frontend; Certificate: 11 backend + 7 frontend) plus a live curl walkthrough against the running dev server, not a full cross-cutting a11y/performance/notification pass or the full 9-story `quickstart.md`.

---

## Dependencies & Execution Order

- **Setup → Foundational** blocks all stories; Foundational depends on `001`/`002`/`003`'s prior work.
- **P1 stories**: US1 (enrollment) is the hard P0 gate for everything else — no lesson, quiz, assignment, or certificate work is meaningful without a verified entitlement path. Recommended order: US1 → US2 (lesson consumption) → US3 (quiz) and US4 (assignment) in parallel (both depend on US2's completion-rule infrastructure but not on each other) → US5 (certificate, depends on US2/US3/US4's eligibility signals).
- **Phase 7b (Discovery & Recommendations)** depends on Foundational + US2 (needs progress data to recommend against) — may run in parallel with US3–US5.
- **P2 stories (US6, US7, US8)** depend on Foundational; US6 (drip/prerequisite) mostly hardens Foundational's T014 evaluator and can run early; US7 (course builder) and US8 (cloning) both depend on US7's T015 workflow state machine, so sequence US7 before US8.
- **Phase 10b (Learning Analytics)** depends on US2/US3/US4's event emission — sequence after the P1 slice.
- **P3 story (US9)** depends on Foundational, US4 (peer review extends assignment review), and `008`'s AI platform being available.
- **Polish (Phase 12)** depends on all desired stories being complete.

## Implementation Strategy

**MVP First**: Setup → Foundational → US1 (enrollment) → **STOP and VALIDATE** entitlement gate works across every source → US2 (lessons) → US3 + US4 in parallel → US5 (certificate) → **STOP and VALIDATE** the full enroll-to-certificate journey (the platform's core credibility deliverable) → then Phase 7b (discovery) → US6/US7/US8 (P2 growth capabilities) → Phase 10b (analytics) → US9 (P3 AI/integrity) → Polish.
