{
  "name": "@coachx/database",
  "version": "0.1.0",
  "private": true,
  "description": "CoachX Enterprise Platform — Prisma schema, migrations, and seed scripts (single source of database truth).",
  "scripts": {
    "generate": "prisma generate",
    "validate": "prisma validate",
    "format": "prisma format",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "migrate:status": "prisma migrate status",
    "studio": "prisma studio",
    "seed": "tsx seeds/index.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.18.0"
  },
  "devDependencies": {
    "prisma": "^5.18.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4"
  },
  "prisma": {
    "seed": "tsx seeds/index.ts"
  }
}
